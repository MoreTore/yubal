# Yubal Configuration

# Server
YUBAL_HOST=0.0.0.0           # Bind address (default: 127.0.0.1)
YUBAL_PORT=8000              # API/web server port (default: 8000)
YUBAL_RELOAD=false           # Auto-reload on code changes (default: false)
YUBAL_DEBUG=false            # Enable debug logging (default: false)

# Paths
YUBAL_DATA_DIR=./data        # Music library output (default: ./data)
YUBAL_BEETS_DIR=./beets      # Beets config and database (default: ./beets)
YUBAL_YTDLP_DIR=./ytdlp      # yt-dlp cache and cookies (default: ./ytdlp)
YUBAL_TEMP_DIR=/tmp/yubal    # Temp directory for downloads (default: <system-temp-dir>/yubal)

# Audio
YUBAL_AUDIO_FORMAT=opus      # opus, m4a, mp3, flac, best (default: opus)
YUBAL_AUDIO_QUALITY=0        # 0 (best) to 10 (worst) (default: 0)

# Timezone
YUBAL_TZ=UTC                 # IANA format (default: UTC)
